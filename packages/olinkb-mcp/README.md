# OlinKB MCP Addon

This package adds the optional MCP transport dependencies required for `olinkb mcp`.

Install the base CLI first, then install or inject this addon only when VS Code MCP integration is required.