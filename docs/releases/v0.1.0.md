# OlinKB v0.1.0

Initial public release of the OlinKB foundation slice.

## Included

- MCP server with the core team-memory tools
- PostgreSQL-backed storage and migration runner
- Local session manager and TTL read cache
- One-shot initialization with `olinkb --init`
- GitHub Release packaging for wheel and source distribution

## Developer Installation

1. Download `olinkb-0.1.0-py3-none-any.whl` from this release.
2. Install it with `pipx`:

```bash
pipx install ./olinkb-0.1.0-py3-none-any.whl
```

3. Initialize OlinKB and choose repository or global installation:

```bash
olinkb --init
```

4. Verify the CLI is available:

```bash
olinkb --help
```

## Notes

- This release does not include semantic retrieval or `pgvector`; the current runtime is PostgreSQL plus `pg_trgm`.
- The developer needs only Python, `pipx`, and access to the external PostgreSQL connection string.